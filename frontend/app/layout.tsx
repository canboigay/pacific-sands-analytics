export const metadata = {
  title: 'Pacific Sands Analytics',
  description: 'Revenue Management Dashboard',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
